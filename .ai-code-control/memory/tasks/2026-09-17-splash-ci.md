# Splash screen and cross-platform CI repair

Date: 2026-09-17
Review: authoring agent reviewed the diff, image placement and regression tests; no independent review claimed.

## Changes
- Promoted the approved Romanian illustration to the README splash and removed its duplicate from the current-architecture section, preserving Mermaid and explanations.
- Documented the distinction between the current raster artwork and the older vector generator.
- Diagnosed failed CI run 35241880821: installer verification depended on an installed Codex executable; offline package installation required uncached registry metadata; macOS temporary-directory aliases failed canonical-path containment.
- Added metadata-only doctor fixtures which refuse execution; used them only in installer and ZIP smoke tests. Production readiness checks remain unchanged.
- Reused reviewed package-lock resolutions for a fresh offline artifact installation, preserving lifecycle-script restrictions.
- Compared canonical repository and target paths, with regression coverage for repository aliases and escaping directory links.
- Preserved executable file permissions when extracting the committed ZIP.
- Preserved the allowlisted host `PATH` for trusted-host quality gates even when
  a gate supplies an explicit empty environment; unapproved variables remain removed.
- Resolved Windows `.cmd`/`.bat` provider shims before adapter doctor checks.
- Made dependency cherry-picks use the worker's bounded Git identity instead of
  relying on a runner-wide Git configuration.
- Canonicalized the package-smoke workspace so npm 11 sees one macOS temp path
  rather than the `/var` and `/private/var` aliases as different dependencies.

## Validation
- Root npm test: 81 passed, zero failed (includes full bundle build).
- Production regression suite: 8 passed; offline package smoke passed.
- Full worker suite: 460 passed, zero failed; JSON validation passed for 75 files.
- Installer verification regression passed with the metadata-only Codex fixture.
- Module provenance, generic boundary and git diff --check passed.
- Code graph inspected diagnostics, path containment and CLI test callers; JavaScript script symbols were not indexed, so direct inspection was used without claiming graph evidence.
- Trace query returned not_found with an empty graph and no fallback; no trace sources are declared.
- Remote six-job CI validation pending at this checkpoint.
