# ICM-06 - internal pilot, migration checks, and release gate

Status: internal implementation and deterministic validation passed on 2026-08-28;
usage-consuming live release pilot remains explicitly pending.

## Outcome

- The internal value gate now runs 20 generic tasks split evenly across contract,
  code-and-tests, documentation-and-review, and semantic-invalidation categories.
- All 20 tasks must complete planner proposal, schema/lint inspection, worker compile,
  fake execution, evidence generation, and semantic source-map enforcement.
- The invalidation group verifies behavior, not only snapshot shape: unchanged inputs
  remain reusable and changing only the policy hash yields exactly `POLICY_CHANGED`.
- The gate reports deterministic structural thresholds and distinguishes
  `INTERNAL_PASS_LIVE_REQUIRED` from `LIVE_PASS`.
- Root documentation and value-gate fixtures now declare criterion-to-gate links
  required by the traceable v1.1 contract.
- The generic bundle boundary was restored by removing consumer-project names from
  bundled planning documents.
- Work moved from protected `main` to `feat/icm-graph-engineering`; no commit was made.

## Pilot result

- DONE tasks: 20/20, with 5/5 in each category.
- Trace coverage: 100%.
- Direct evidence: 100%.
- First-pass gate rate: 100%.
- Semantic invalidation checks: 5/5.
- Internal release decision: `INTERNAL_PASS_LIVE_REQUIRED`.

The fake-engine matrix validates contracts, orchestration, evidence, source maps, and
invalidation deterministically. It does not establish live provider cost, uncached input
reduction, retry reduction, or human time-to-explanation. The already recorded root TODO
for the usage-consuming Claude pilot therefore remains open.

## Validation

- Root build and tests: PASS (3/3).
- AI Code Worker: 379/379 tests PASS and 71 JSON files valid.
- AI Code Planner: 35/35 tests PASS and 18 JSON files valid.
- AI Code Control: 69/69 tests PASS.
- Internal value, review, and docs gates: PASS.
- Generic bundle boundary: PASS.
- `verify-changed-files`: PASS on `feat/icm-graph-engineering` with no unexpected,
  forbidden, or conflicting files.
- `run-validation`: PASS for .NET build/test and MCP install/build.

## Measurement

The preregistered range was 2.5M / 3.4M / 4.5M reported tokens. Actual usage was
3,118,860 tokens, 8.27% below the median and inside the range (GREEN). The clean,
same-session usage delta was 14 percentage points, or approximately 222,776 tokens per
point. No parallel Codex sessions were detected.

The final ICM report is RED overall: six token-comparable stages have MAPE 174.2% and
33.3% range hits. Four stages have clean usage mapping; their median is 246,623
tokens per percentage point. Calibration `ICM-CLEAN-V1-2026-08-28` is prospective for
Graph Engineering only and does not rewrite preregistered ICM estimates.
