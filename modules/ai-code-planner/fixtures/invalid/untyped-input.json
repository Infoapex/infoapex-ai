{
  "goal": "Fixture for UNTYPED_INPUT linter rule (REQUIRED_INPUTS_TYPED check).",
  "tasks": [
    {
      "id": "TASK-A",
      "goal": "Task A has a requiredInputs entry missing the 'kind' discriminant.",
      "acceptanceCriteria": [
        { "criterionId": "AC-001", "text": "Output file exists." }
      ],
      "gates": [
        { "gateId": "G-001", "command": "verify-a", "evidenceContract": "Exit code 0." }
      ],
      "dependsOn": [],
      "scope": {
        "allowedPaths": ["src/a/**"],
        "forbiddenPaths": []
      },
      "requiredInputs": [
        { "ref": "docs/PHASE-0.md" }
      ]
    }
  ]
}
