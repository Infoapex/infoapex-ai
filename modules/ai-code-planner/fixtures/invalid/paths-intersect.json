{
  "goal": "Fixture for SCOPE_SELF_INTERSECTION linter rule (SCOPE_NO_SELF_INTERSECTION check).",
  "tasks": [
    {
      "id": "TASK-A",
      "goal": "Task A lists src/shared/** in both allowedPaths and forbiddenPaths.",
      "acceptanceCriteria": [
        { "criterionId": "AC-001", "text": "Output file exists." }
      ],
      "gates": [
        { "gateId": "G-001", "command": "verify-a", "evidenceContract": "Exit code 0.", "criterionIds": ["AC-001"] }
      ],
      "dependsOn": [],
      "scope": {
        "allowedPaths": ["src/a/**", "src/shared/**"],
        "forbiddenPaths": ["src/shared/**"]
      },
      "requiredInputs": []
    }
  ]
}
