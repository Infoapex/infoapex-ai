# generic-v1

This CI suite has exactly twelve public task manifests. The `evaluator-oracles/` paths
in those manifests are opaque references; this distributable directory deliberately
contains no oracle payloads, solutions, or expected diffs.
