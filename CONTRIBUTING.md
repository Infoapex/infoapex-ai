# Contributing

Use a bounded task, preserve module CLI/JSON boundaries, add negative tests for every
security or recovery change, and run the relevant standalone suite plus the root suite.
Generated evidence must identify its commit and must not overwrite an earlier run.
