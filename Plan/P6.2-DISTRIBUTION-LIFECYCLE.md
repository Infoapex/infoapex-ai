---
status: accepted
---

# P6.2 distribution, installation, upgrade, and rollback hardening

```json ai-code-worker-plan
{
  "goal": "Harden the P6.2 distribution and release lifecycle so the committed bundle can be checked, packaged, installed, upgraded, and rolled back with bounded, reproducible evidence.",
  "tasks": [
    {
      "id": "P6.2-LIFECYCLE-01",
      "kind": "contract",
      "role": "p6-release-worker",
      "dependsOn": [],
      "requiredInputs": [
        "src/release-lifecycle.ts",
        "src/full-install.ts",
        "tests/release-lifecycle.test.ts",
        "scripts/check-npm-package.mjs",
        "scripts/package-smoke-test.mjs",
        "docs/plans/INFOAPEX-AI-P6-PRODUCTION-READINESS-PLAN.md"
      ],
      "allowedPaths": [
        "src/release-lifecycle.ts",
        "src/full-install.ts",
        "tests/release-lifecycle.test.ts",
        "scripts/check-npm-package.mjs",
        "scripts/package-smoke-test.mjs",
        "tests/package-lifecycle.test.ts"
      ],
      "forbiddenPaths": [
        ".git/**",
        "dist/**",
        "dist-release/**",
        "validation/**"
      ],
      "expectedArtifacts": [
        "dist/src/cli.js",
        "modules/provenance.json"
      ],
      "acceptanceCriteria": [
        "Release checks reject unsafe repository or bundle paths before reading or writing outside the intended roots.",
        "Upgrade journals validate identifiers, paths, schema, backups, and target state before restore; repeated check and dry-run operations remain stable.",
        "Clean package smoke exercises the npm artifact entry point and a disposable target without relying on source-checkout files or hidden runtime downloads.",
        "Package content checks reject local state, tests, validation data, node_modules, and untracked development artifacts while requiring all supported runtime entries.",
        "Regression coverage includes missing/invalid profiles, bundle mismatch, malformed journals, changed targets, clean artifact install, and rollback safety.",
        "Before local verification, dependencies are bootstrapped with npm ci --ignore-scripts; ignored node_modules content is runtime-only and must not be committed."
      ],
      "verify": [
        "npm ci --ignore-scripts",
        "root-tests",
        "generic-boundary",
        "release-package-check",
        "release-package-smoke"
      ],
      "concurrencyKeys": ["p6-release-lifecycle"],
      "risk": "high"
    }
  ],
  "globalGates": [
    "npm ci --ignore-scripts",
    "root-tests",
    "generic-boundary",
    "release-package-check",
    "release-package-smoke"
  ],
  "budgets": {
    "maximumParallelWriters": 1,
    "maximumRepairCycles": 0,
    "maximumTaskMinutes": 20,
    "maximumRunMinutes": 30,
    "maximumAgentInvocations": 1,
    "maximumRunInputUncachedTokens": 120000,
    "maximumRunCacheReadTokens": 3000000,
    "maximumRunCacheWriteTokens": 100000,
    "maximumRunOutputTokens": 32000,
    "maximumRunCostUsd": null,
    "onUnknownUsage": "block"
  }
}
```
