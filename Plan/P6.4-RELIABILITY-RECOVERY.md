---
status: accepted
---

# P6.4 reliability, recovery, and resource control

```json ai-code-worker-plan
{
  "goal": "Harden core-local execution recovery so interrupted, concurrent, retried, and resource-bounded runs preserve valid state without duplicate effects or permission expansion.",
  "tasks": [
    {
      "id": "P6.4-RECOVERY-01",
      "kind": "contract",
      "role": "p6-reliability-worker",
      "dependsOn": [],
      "requiredInputs": [
        "src/run/fake-run.ts",
        "src/run/codex-run.ts",
        "src/run/claude-run.ts",
        "src/state/event-log.ts",
        "src/state/run-state.ts",
        "src/production.ts",
        "tests/recovery.test.ts",
        "tests/fake-run.test.ts",
        "docs/P6-OPERATIONS.md",
        "docs/plans/INFOAPEX-AI-P6-PRODUCTION-READINESS-PLAN.md"
      ],
      "allowedPaths": [
        "src/run/**",
        "src/state/**",
        "src/production.ts",
        "tests/recovery.test.ts",
        "tests/fake-run.test.ts",
        "tests/reliability-recovery.test.ts",
        "docs/P6-OPERATIONS.md",
        "docs/P6-RECOVERY-MATRIX.md"
      ],
      "forbiddenPaths": [
        ".git/**",
        "dist/**",
        "dist-release/**",
        "validation/**"
      ],
      "expectedArtifacts": [
        "docs/P6-OPERATIONS.md",
        "docs/P6-RECOVERY-MATRIX.md"
      ],
      "acceptanceCriteria": [
        "State, event, manifest, and lease writes are atomic or fail closed with bounded recovery evidence.",
        "Resume, retry, and stale-lease recovery are idempotent and never duplicate task commits, gate effects, or external actions.",
        "Concurrent runs on one repository are serialized or one is rejected explicitly without widening permissions or changing provider silently.",
        "Fault-injection coverage exercises interruption before/after state commits, provider timeout/rate limit, malformed output, worktree corruption, and cleanup interruption.",
        "Resource limits and process-tree cancellation preserve a terminal state and bounded evidence when time/output/process budgets are exceeded.",
        "Operations documentation records recovery invariants, decision codes, cleanup inventory, and operator-safe resume steps.",
        "Before local verification, dependencies are bootstrapped with npm ci --ignore-scripts; ignored node_modules content is runtime-only and must not be committed."
      ],
      "verify": [
        "npm ci --ignore-scripts",
        "root-tests",
        "generic-boundary",
        "release-package-check"
      ],
      "concurrencyKeys": ["p6-reliability-recovery"],
      "risk": "high"
    }
  ],
  "globalGates": [
    "npm ci --ignore-scripts",
    "root-tests",
    "generic-boundary",
    "release-package-check"
  ],
  "budgets": {
    "maximumParallelWriters": 1,
    "maximumRepairCycles": 0,
    "maximumTaskMinutes": 20,
    "maximumRunMinutes": 30,
    "maximumAgentInvocations": 1,
    "maximumRunInputUncachedTokens": 120000,
    "maximumRunCacheReadTokens": 5000000,
    "maximumRunCacheWriteTokens": 100000,
    "maximumRunOutputTokens": 40000,
    "maximumRunCostUsd": null,
    "onUnknownUsage": "block"
  }
}
```
