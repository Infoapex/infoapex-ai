---
status: accepted
---

# P6.1 configuration contract and migration hardening

```json ai-code-worker-plan
{
  "goal": "Harden the P6.1 configuration lifecycle so supported config contracts are validated and migrations are safe, idempotent, path-bounded, and recoverable.",
  "tasks": [
    {
      "id": "P6.1-CONFIG-01",
      "kind": "contract",
      "role": "p6-lifecycle-worker",
      "dependsOn": [],
      "requiredInputs": [
        "src/config-lifecycle.ts",
        "tests/config-lifecycle.test.ts",
        "src/cli.ts",
        "docs/plans/INFOAPEX-AI-P6-PRODUCTION-READINESS-PLAN.md"
      ],
      "allowedPaths": [
        "src/config-lifecycle.ts",
        "tests/config-lifecycle.test.ts",
        "src/cli.ts",
        "tests/cli.test.ts"
      ],
      "forbiddenPaths": [
        ".git/**",
        "node_modules/**",
        "dist/**",
        "dist-release/**",
        "validation/**"
      ],
      "expectedArtifacts": [
        "dist/src/cli.js"
      ],
      "acceptanceCriteria": [
        "Configuration lifecycle operations reject unsafe repository-relative paths and backup paths before reading or writing outside the repository.",
        "Migration check, dry-run, apply, and rollback return stable machine-readable statuses and remain idempotent across repeated invocations.",
        "Migration journals and backups are verified before restore, and a changed target is not silently overwritten during rollback.",
        "Future schema versions and malformed migration journals fail closed with actionable bounded diagnostics.",
        "Regression tests cover path traversal, malformed journals, idempotent apply, dry-run, verified rollback, and unchanged existing CLI behavior."
      ],
      "verify": [
        "npm ci --ignore-scripts",
        "npm test",
        "npm run check:generic-boundary"
      ],
      "concurrencyKeys": [
        "p6-config-lifecycle"
      ],
      "risk": "high"
    }
  ],
  "globalGates": [
    "npm ci --ignore-scripts",
    "npm test",
    "npm run check:generic-boundary"
  ],
  "budgets": {
    "maximumParallelWriters": 1,
    "maximumRepairCycles": 0,
    "maximumTaskMinutes": 20,
    "maximumRunMinutes": 30,
    "maximumAgentInvocations": 1,
    "maximumRunInputUncachedTokens": 120000,
    "maximumRunCacheReadTokens": 400000,
    "maximumRunCacheWriteTokens": 100000,
    "maximumRunOutputTokens": 24000,
    "maximumRunCostUsd": null,
    "onUnknownUsage": "block"
  }
}
```
