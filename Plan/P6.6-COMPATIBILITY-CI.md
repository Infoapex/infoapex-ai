---
status: accepted
---

# P6.6 compatibility, quality, and release CI

```json ai-code-worker-plan
{
  "goal": "Automate the supported core-local compatibility promise across declared platforms, runtimes, providers, contract boundaries, and release artifacts.",
  "tasks": [
    {
      "id": "P6.6-COMPAT-01",
      "kind": "test",
      "role": "p6-compatibility-worker",
      "dependsOn": [],
      "requiredInputs": [
        ".github/workflows/ci.yml",
        ".github/workflows/release.yml",
        "scripts/release-smoke-test.mjs",
        "scripts/check-npm-package.mjs",
        "scripts/package-smoke-test.mjs",
        "tests/delegate.test.ts",
        "docs/plans/INFOAPEX-AI-P6-PRODUCTION-READINESS-PLAN.md"
      ],
      "allowedPaths": [
        ".github/workflows/**",
        ".github/dependabot.yml",
        "scripts/release-smoke-test.mjs",
        "scripts/check-npm-package.mjs",
        "scripts/package-smoke-test.mjs",
        "scripts/compatibility-check.mjs",
        "tests/delegate.test.ts",
        "tests/compatibility.test.ts",
        "docs/P6-COMPATIBILITY.md"
      ],
      "forbiddenPaths": [
        ".git/**",
        "dist/**",
        "dist-release/**",
        "validation/**"
      ],
      "expectedArtifacts": [
        "docs/P6-COMPATIBILITY.md"
      ],
      "acceptanceCriteria": [
        "CI declares and executes the supported Windows/Linux/macOS matrix without retries masking failures, with Node and provider capability checks before execution.",
        "Release smoke runs only from the committed/package artifact and verifies runtime entry points, provenance, package content, and offline-safe startup behavior.",
        "Compatibility checks fail closed for unsupported runtime/provider versions and record stable machine-readable diagnostics.",
        "Contract, path, quoting, event parsing, and package boundary regressions have explicit tests and do not rely on global coverage percentages.",
        "Documentation states the supported matrix, minimum/recommended runtimes, provider pins, unsupported combinations, and regression policy.",
        "Before local verification, dependencies are bootstrapped with npm ci --ignore-scripts; ignored node_modules content is runtime-only and must not be committed."
      ],
      "verify": [
        "npm ci --ignore-scripts",
        "root-tests",
        "generic-boundary",
        "release-package-check",
        "release-package-smoke"
      ],
      "concurrencyKeys": ["p6-compatibility-ci"],
      "risk": "high"
    }
  ],
  "globalGates": [
    "npm ci --ignore-scripts",
    "root-tests",
    "generic-boundary",
    "release-package-check",
    "release-package-smoke"
  ],
  "budgets": {
    "maximumParallelWriters": 1,
    "maximumRepairCycles": 0,
    "maximumTaskMinutes": 20,
    "maximumRunMinutes": 30,
    "maximumAgentInvocations": 1,
    "maximumRunInputUncachedTokens": 120000,
    "maximumRunCacheReadTokens": 5000000,
    "maximumRunCacheWriteTokens": 100000,
    "maximumRunOutputTokens": 40000,
    "maximumRunCostUsd": null,
    "onUnknownUsage": "block"
  }
}
```
